# HALO provider keys — copy to `.env` (gitignored) and fill in.
# `.env` is loaded at startup; real env (e.g. `infisical run --`) overrides it.

SYNTHETIC_API_KEY=
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# OPENROUTER_API_KEY=
# GEMINI_API_KEY=
# HALO_TOKEN=            # bearer auth; required once bound off loopback (Tailscale)
